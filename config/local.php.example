<?php

declare(strict_types=1);

/**
 * Lokalna konfiguracja — skopiuj jako config/local.php (nie commituj!).
 *
 * Na produkcji OBOWIĄZKOWO nadpisz sekrety z config/security.php:
 * - security.ip_salt  — losowy ciąg min. 32 znaki
 * - security.app_key  — losowy ciąg min. 32 znaki
 *
 * Przykład generowania: php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
 */
return [
    // Dane z panelu hostingu (MySQL)
    'database' => [
        'host'     => '127.0.0.1',
        'port'     => 3306,
        'database' => 'przypierdolka',
        'username' => 'twoj_login_mysql',
        'password' => 'twoje_haslo_mysql',
    ],

    'app' => [
        'env'   => 'production',
        'url'   => 'https://przypierdolka.pl',
        'debug' => false,
    ],

    'security' => [
        'ip_salt' => 'PRODUKCJA_ZMIEN_LOSOWY_SALT',
        'app_key' => 'PRODUKCJA_ZMIEN_LOSOWY_APP_KEY',
    ],
    // Opcjonalnie: łagodniejsze limity na localu
    // 'security' => [
    //     'rate_limits' => [
    //         'login'    => ['max' => 100, 'window' => 60],
    //         'login_ip' => ['max' => 500, 'window' => 60],
    //     ],
    // ],
];
